export const Sizes = {
  large: 'large',
  medium: 'medium',
  small: 'small',
};

export const SIZES_KEYS = Object.keys(Sizes);

export const TEXTAREA_DEFAULT_ROWS = 5;

export const AutocompleteOptions = {
  default: undefined,
  off: 'off',
};

export const AUTOCOMPLETE_OPTIONS_KEYS = Object.keys(AutocompleteOptions);
